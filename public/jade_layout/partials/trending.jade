
div.col-md-6
  table(class="table") 
    tr(ng-repeat="trending in trendings | limitTo:5")
      td.pic
        img(src="{{trending.data.thumbnail}}")
      td.trend
        h2 {{trending.data.title}}
div.col-md-6
  table(class="table") 
    tr(ng-repeat="trending in trendings | limitTo:5")
      td.pic
        img(src="{{trending.data.thumbnail}}")
      td.trend
        h2 {{trending.data.title}}
  
